-- User table need to have : id or uuid (unique key), pseudo (unique),
-- email, password
-- 1aLoginTime, lastLoginTime, playedTime

-- Succes table have : sid, ... (all other info will be in Java (images, name & desc translated))

-- UnlockedSucces table have : sid, playerUuid, unlockedTime

-- Whith this we will be able to connect as user + print all user succes + print number of user + print %age unlocked for all succes.

-- Add score as a table ? with score, playerUuid, map, time
