name: updateVersion

on:
  pull_request:
    branches: [ master ]
    # types: [closed]

jobs:
  updateVersionJob:
    # if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    # outputs:
      # output1: ${{ steps.create_release.outputs.upload_url }}
    steps:
    - name: checkout
      uses: actions/checkout@v2
    - name: chmod
      run: |
        chmod 700 *.sh
        echo "-------------"
        echo $(./getLastGitBranch.sh)
        echo "-------------"
        echo $(git branch)
        echo "-------------"
    - name: ini git
      run: |
        # git checkout master
        ls
        git checkout refs/heads/$(./getLastGitBranch.sh)
        git pull
    - name: updateVersion.sh
      run: ./updateVersion.sh
    - name: commit version.md
      run: |
        if [[ `git status --untracked-files=no --porcelain docs/` != "\n" ]]; then
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add version.md
          git commit -m "Auto version.md update"
          git push
        fi
